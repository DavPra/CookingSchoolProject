package Cooking.School.Project.cookingSchool.notes;


/*

public class notes {

     for (User user : course.getUsers()) {
        UserDTO userDTO = new UserDTO();
        userDTO.setUserId(user.getUserId());
        this.userIds.add(userDTO);
    }
}

}

class UserDTO {

    @Getter
    long userId;

    public void setUserId(long userId) {
        this.userId = userId;
    }
}

 */